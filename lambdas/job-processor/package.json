{
  "name": "job-processor-lambda",
  "version": "1.0.0",
  "description": "Generic Lambda function for processing async jobs",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run generate && tsc && ./build.sh",
    "generate": "cd ../../apps/api && npx prisma generate && cd ../../lambdas/job-processor && mkdir -p node_modules/@prisma && cp -r ../../node_modules/@prisma/client node_modules/@prisma/client && cp -r ../../node_modules/.prisma node_modules/",
    "test": "jest",
    "clean": "rm -rf dist *.zip",
    "postinstall": "npm run generate 2>/dev/null || true"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.400.0",
    "@prisma/client": "^5.8.1",
    "papaparse": "^5.5.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.159",
    "@types/jest": "^29.5.0",
    "@types/node": "^20.11.0",
    "@types/papaparse": "^5.5.1",
    "jest": "^29.7.0",
    "prisma": "^5.8.1",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.3"
  }
}
